import ShoppingCart from "../../Components/ShoppingCart";

const Header = () => {
  return (
    <header className="w-screen flex">
      <ShoppingCart />
    </header>
  );
};

export default Header;
